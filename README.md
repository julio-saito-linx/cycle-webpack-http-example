# Cycle.js http example with webpack

## Install and Watch

```sh
npm install
npm start
```

- open http://localhost:3000

### publish to `surge.sh`

```sh
npm install --global surge
npm run publish
```

